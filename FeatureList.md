# Info-Droplets Rev4 Feature List

## Droplet ID

This ID is written once during initial setup and read on every startup to uniquely identify the device.
It is a unique byte value stored in the EEPROM at address `0`.